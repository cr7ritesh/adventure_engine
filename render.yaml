services:
  - type: web
    name: puch_ai_mcp
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: AUTH_TOKEN
        sync: false
      - key: MY_NUMBER
        sync: false
      - key: GEMINI_API_KEY
        sync: false
